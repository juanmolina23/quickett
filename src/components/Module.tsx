import { Card, Container } from "react-bootstrap";
import ResultSet from "./ResultSet";

const Module = () => {
  return (
    <Card>
      <Card.Body>
        <ResultSet />
      </Card.Body>
    </Card>
  );
};

export default Module;
